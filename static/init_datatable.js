document.addEventListener('DOMContentLoaded', function () {
    let table = new DataTable('#TableIO', {
        paging: true,
        order: false,
        searching: true,
        colReorder: true,
        autoWidth: false,
    });
});